#include <stdio.h>

/**
 * main prints some letters of alphabet
 *
 * Return:Always(Success)
 */
int main(void)
{

	char c;

	for(c='a'; c <= 'z'; c++)
	{
		if(!= 'e' && c != 'q')
			putchar(c);
	}
}

putchar('\n')

return (O);
}
		

	



