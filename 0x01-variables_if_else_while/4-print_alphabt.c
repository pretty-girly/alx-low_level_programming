#include <stdio.h>

/**
 * main - prints some letters of alphabet
 *
 * Return:Always(Success)
 */
int main(void)
{
/* This is where our code starts */
	char ca;

	for (ca = 'a'; ca <= 'z'; ca++)
	{
		if (ca != 'e' && ca != 'q')
			putchar(ca);
	}
putchar('\n')

return (O);
}
